# 新盟教育-Linux运维RHCSA+RHCE培训教程：P52：常见协议及端口介绍、firewalld防火墙 🔥

在本节课中，我们将要学习网络中的常见协议及其对应的端口号，并深入理解防火墙的概念。我们将以红帽系统中的 `firewalld` 防火墙为例，学习其基本工作原理和配置方法，为后续的网络安全管理打下基础。

## 协议与端口：网络世界的规则与门牌号 🚦

![](img/ffe34928916ec278c70e79706449b39c_1.png)

在互联网中，数据传输需要遵循特定的规则，这些规则被称为“协议”。协议规定了数据如何打包、传输和接收。同时，为了区分服务器上运行的不同应用程序，我们引入了“端口”的概念。

### 常见协议介绍

![](img/ffe34928916ec278c70e79706449b39c_3.png)

以下是网络通信中一些典型的协议：

![](img/ffe34928916ec278c70e79706449b39c_5.png)

![](img/ffe34928916ec278c70e79706449b39c_7.png)

*   **HTTP (超文本传输协议)**：用于传输网页内容的协议。它是**明文协议**，意味着传输的数据可以被第三方截获并解读。
    *   **默认端口**：`80`
*   **HTTPS (安全的超文本传输协议)**：在HTTP基础上增加了SSL/TLS加密层，确保数据传输的安全性。它是**加密协议**，即使数据包被截获，也无法轻易解读。
    *   **默认端口**：`443`
*   **FTP (文件传输协议)**：用于在网络上进行文件传输的协议，类似于一个内部网盘。
    *   **默认端口**：`21`
*   **SSH (安全外壳协议)**：用于远程安全登录和管理服务器的协议，替代了不安全的Telnet。
    *   **默认端口**：`22`
*   **DNS (域名系统协议)**：用于将域名解析为IP地址。
    *   **默认端口**：`53`

### 端口的作用

端口是网络通信的端点，可以理解为服务器上每个应用程序的“门牌号”。当用户访问服务器时，通过IP地址找到服务器，再通过特定的端口号访问服务器上对应的服务。

例如，一台服务器同时运行着网站（HTTP服务）和文件共享（FTP服务）：
*   用户想访问网站，其请求会指向服务器的 `80` 端口。
*   用户想上传文件，其请求会指向服务器的 `21` 端口。

这样，通过不同的端口号，服务器就能正确地将用户请求分发给对应的应用程序。

### 查看协议与端口

在Linux系统中，可以通过 `/etc/services` 文件查看众多协议及其默认端口的映射关系。

```bash
cat /etc/services | grep -E “(http|https|ssh)”
```

![](img/ffe34928916ec278c70e79706449b39c_9.png)

![](img/ffe34928916ec278c70e79706449b39c_11.png)

此外，可以使用 `ss` 命令查看当前系统正在监听的端口及其对应的服务。

![](img/ffe34928916ec278c70e79706449b39c_13.png)

```bash
ss -ntlp
```

上一节我们介绍了网络协议和端口的基本概念，本节中我们来看看如何通过防火墙来管理对这些端口的访问。

![](img/ffe34928916ec278c70e79706449b39c_15.png)

![](img/ffe34928916ec278c70e79706449b39c_17.png)

## 防火墙：网络边界的安全卫士 🛡️

![](img/ffe34928916ec278c70e79706449b39c_19.png)

![](img/ffe34928916ec278c70e79706449b39c_21.png)

防火墙是一种网络安全系统，它根据预定义的规则，监控并控制进出网络或主机的数据流量。

![](img/ffe34928916ec278c70e79706449b39c_23.png)

### 防火墙的核心概念

![](img/ffe34928916ec278c70e79706449b39c_24.png)

![](img/ffe34928916ec278c70e79706449b39c_26.png)

防火墙工作在主机或网络的边缘，对于进出本主机或网络的数据报文，会根据事先设定好的检查规则进行匹配检测。对于能够被规则匹配到的报文，会做出相应的处理（如允许通过或拒绝）。

![](img/ffe34928916ec278c70e79706449b39c_28.png)

![](img/ffe34928916ec278c70e79706449b39c_30.png)

### 防火墙的种类

![](img/ffe34928916ec278c70e79706449b39c_32.png)

防火墙主要分为两大类：

![](img/ffe34928916ec278c70e79706449b39c_34.png)

![](img/ffe34928916ec278c70e79706449b39c_36.png)

1.  **软件防火墙**：通过安装在操作系统上的软件来实现包过滤功能，例如Linux系统中的 `iptables` 和 `firewalld`。成本低，配置灵活。
2.  **硬件防火墙**：一个独立的物理设备，专门用于处理网络边界的安全策略。性能高，通常提供图形化配置界面，但价格昂贵。

### 防火墙的类型

根据保护范围，又可分为：

*   **主机防火墙**：保护单台主机，规则仅应用于本机。适用于服务器数量较少的环境。
*   **网络防火墙**：保护整个网络，通常部署在网络入口处，为内部所有主机提供安全防护。适用于中大型企业网络。

在RHEL/CentOS 7及之后版本中，默认的防火墙管理工具是 `firewalld`，而RHEL/CentOS 6及之前版本使用的是 `iptables`。`firewalld` 底层仍然调用 `iptables` 的规则，但提供了更动态、更易管理的配置方式。

接下来，我们将通过实际操作来学习 `firewalld` 的基本用法。

## 实战：配置firewalld防火墙 ⚙️

我们将通过安装一个Web服务（HTTPD）来演示 `firewalld` 的配置。

![](img/ffe34928916ec278c70e79706449b39c_38.png)

### 1. 安装并启动Web服务

![](img/ffe34928916ec278c70e79706449b39c_40.png)

首先，安装Apache HTTP服务器（httpd）。

![](img/ffe34928916ec278c70e79706449b39c_42.png)

```bash
yum -y install httpd
systemctl start httpd
systemctl enable httpd
```

![](img/ffe34928916ec278c70e79706449b39c_44.png)

![](img/ffe34928916ec278c70e79706449b39c_46.png)

使用 `ss` 命令确认httpd服务正在监听80端口。

![](img/ffe34928916ec278c70e79706449b39c_48.png)

```bash
ss -ntlp | grep httpd
```

![](img/ffe34928916ec278c70e79706449b39c_50.png)

![](img/ffe34928916ec278c70e79706449b39c_52.png)

### 2. 理解firewalld的区域（Zone）

![](img/ffe34928916ec278c70e79706449b39c_54.png)

![](img/ffe34928916ec278c70e79706449b39c_55.png)

`firewalld` 使用“区域（Zone）”来管理规则。每个区域是一套预定义的规则集，适用于不同的信任级别。常见的区域有：

![](img/ffe34928916ec278c70e79706449b39c_57.png)

*   **public（默认）**：仅允许访问本机的SSH、DHCP等少量服务。
*   **trusted**：允许所有访问。
*   **block**：拒绝所有访问，但会明确回复“拒绝”。
*   **drop**：丢弃所有访问请求，不做任何回复，使访问者感觉像超时。

![](img/ffe34928916ec278c70e79706449b39c_58.png)

![](img/ffe34928916ec278c70e79706449b39c_60.png)

![](img/ffe34928916ec278c70e79706449b39c_62.png)

查看当前默认区域：

![](img/ffe34928916ec278c70e79706449b39c_64.png)

![](img/ffe34928916ec278c70e79706449b39c_66.png)

![](img/ffe34928916ec278c70e79706449b39c_68.png)

```bash
firewall-cmd --get-default-zone
```

更改默认区域（例如改为 trusted）：

![](img/ffe34928916ec278c70e79706449b39c_70.png)

```bash
firewall-cmd --set-default-zone=trusted
```

![](img/ffe34928916ec278c70e79706449b39c_72.png)

![](img/ffe34928916ec278c70e79706449b39c_74.png)

更改区域后，尝试访问Web服务，观察其变化。

![](img/ffe34928916ec278c70e79706449b39c_76.png)

![](img/ffe34928916ec278c70e79706449b39c_78.png)

### 3. 为区域添加服务规则

![](img/ffe34928916ec278c70e79706449b39c_80.png)

![](img/ffe34928916ec278c70e79706449b39c_82.png)

将默认区域改回 `public`。此时，由于 `public` 区域默认不允许HTTP服务，所以无法访问网页。

![](img/ffe34928916ec278c70e79706449b39c_84.png)

![](img/ffe34928916ec278c70e79706449b39c_86.png)

我们需要向 `public` 区域添加允许HTTP服务的规则。

![](img/ffe34928916ec278c70e79706449b39c_88.png)

![](img/ffe34928916ec278c70e79706449b39c_90.png)

![](img/ffe34928916ec278c70e79706449b39c_92.png)

```bash
firewall-cmd --set-default-zone=public
firewall-cmd --zone=public --add-service=http --permanent
firewall-cmd --reload
```

命令解释：
*   `--zone=public`：指定操作的区域。
*   `--add-service=http`：添加http服务（对应80端口）。
*   `--permanent`：将规则永久保存，否则重启后失效。
*   `--reload`：重新加载防火墙配置，使永久规则立即生效。

现在，再次访问Web服务，应该可以成功打开页面。

### 4. 查看和管理规则

查看 `public` 区域的所有规则：

```bash
firewall-cmd --zone=public --list-all
```

![](img/ffe34928916ec278c70e79706449b39c_94.png)

![](img/ffe34928916ec278c70e79706449b39c_96.png)

从区域中移除某个服务规则（例如移除https）：

```bash
firewall-cmd --zone=public --remove-service=https --permanent
firewall-cmd --reload
```

## 总结 📚

本节课中我们一起学习了：
1.  **网络协议与端口**：理解了HTTP、HTTPS、SSH等常见协议的作用及其默认端口，知道了端口是访问服务器上特定服务的“门牌号”。
2.  **防火墙基础**：掌握了防火墙作为网络安全边界卫士的工作原理，了解了软件防火墙与硬件防火墙、主机防火墙与网络防火墙的区别。
3.  **firewalld实战**：学会了使用 `firewall-cmd` 工具管理 `firewalld` 防火墙，包括查看和更改默认区域、为区域添加或移除服务规则等基本操作。

![](img/ffe34928916ec278c70e79706449b39c_98.png)

![](img/ffe34928916ec278c70e79706449b39c_99.png)

![](img/ffe34928916ec278c70e79706449b39c_101.png)

防火墙是Linux系统安全管理中至关重要的一环。虽然 `firewalld` 提供了更上层的管理方式，但在实际生产环境中，传统的 `iptables` 因其直接和强大仍然被广泛使用。在接下来的课程中，我们将进一步学习 `iptables` 的配置。