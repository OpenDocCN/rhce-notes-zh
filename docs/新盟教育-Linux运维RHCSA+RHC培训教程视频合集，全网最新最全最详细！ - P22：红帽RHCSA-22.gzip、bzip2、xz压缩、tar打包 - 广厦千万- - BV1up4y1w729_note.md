# Linux运维RHCSA+RHCE培训教程：P22：gzip、bzip2、xz压缩与tar打包 📦

![](img/d23d693b165264e1f2745b298c09e9ab_1.png)

在本节课中，我们将要学习Linux系统中常用的文件压缩与打包工具。我们将了解`gzip`、`bzip2`、`xz`这三种压缩格式的特点与用法，并掌握`tar`打包工具如何将多个文件或目录整合并压缩，这对于文件备份、传输和节省磁盘空间至关重要。

## 压缩基础概念 💡

![](img/d23d693b165264e1f2745b298c09e9ab_3.png)

压缩的主要目的是**节约磁盘空间**和**提高文件传输速度**。其原理类似于将松散的文件内容变得更紧密。例如，互联网上的图片（如.jpg）、音频（如.mp3）、视频（如.mp4）文件通常都是压缩格式，以便快速传输和加载。

压缩过程涉及**压缩速度**与**压缩比例**的权衡：
*   **压缩速度快**，则压缩后的文件体积相对较大。
*   **压缩比例高**（即压缩后体积小），则压缩速度通常较慢。

![](img/d23d693b165264e1f2745b298c09e9ab_5.png)

![](img/d23d693b165264e1f2745b298c09e9ab_7.png)

## Linux下的压缩工具 🔧

![](img/d23d693b165264e1f2745b298c09e9ab_9.png)

![](img/d23d693b165264e1f2745b298c09e9ab_11.png)

![](img/d23d693b165264e1f2745b298c09e9ab_13.png)

Linux系统提供了多种压缩工具，我们将重点学习`gzip`、`bzip2`和`xz`。它们各有特点，适用于不同场景。

### 准备工作

首先，我们创建一个测试环境。进入`/opt`目录，清空内容，并复制一个较大的文件（如`/etc/services`）用于测试。

![](img/d23d693b165264e1f2745b298c09e9ab_15.png)

![](img/d23d693b165264e1f2745b298c09e9ab_17.png)

```bash
cd /opt
rm -rf *
cp /etc/services .
ls -lh services
```
命令 `du -h services` 可以查看文件大小，例如显示为 **656K**。

![](img/d23d693b165264e1f2745b298c09e9ab_19.png)

### 1. gzip压缩格式

![](img/d23d693b165264e1f2745b298c09e9ab_20.png)

![](img/d23d693b165264e1f2745b298c09e9ab_22.png)

![](img/d23d693b165264e1f2745b298c09e9ab_23.png)

`gzip`是Linux下最常用、压缩速度最快的工具之一。

*   **压缩文件**：命令后直接跟文件名即可。
    ```bash
    gzip services
    ```
    压缩后，原文件`services`会变成`services.gz`。使用 `du -h services.gz` 查看，大小可能变为 **136K**。

![](img/d23d693b165264e1f2745b298c09e9ab_25.png)

*   **查看压缩文件内容**：不能使用`cat`，需使用`zcat`。
    ```bash
    zcat services.gz
    ```

![](img/d23d693b165264e1f2745b298c09e9ab_27.png)

![](img/d23d693b165264e1f2745b298c09e9ab_29.png)

*   **解压缩文件**：使用 `-d` 选项。
    ```bash
    gzip -d services.gz
    ```
    解压后文件恢复原名及原始大小。

![](img/d23d693b165264e1f2745b298c09e9ab_31.png)

![](img/d23d693b165264e1f2745b298c09e9ab_33.png)

### 2. bzip2压缩格式

`bzip2`的压缩比例通常比`gzip`更高，但速度稍慢。如果系统未安装，可使用 `yum install bzip2 -y` 安装。

![](img/d23d693b165264e1f2745b298c09e9ab_35.png)

![](img/d23d693b165264e1f2745b298c09e9ab_36.png)

*   **压缩文件**：
    ```bash
    bzip2 services
    ```
    压缩后文件为`services.bz2`。查看大小可能为 **124K**。

![](img/d23d693b165264e1f2745b298c09e9ab_38.png)

![](img/d23d693b165264e1f2745b298c09e9ab_39.png)

![](img/d23d693b165264e1f2745b298c09e9ab_41.png)

*   **查看压缩文件内容**：使用`bzcat`。
    ```bash
    bzcat services.bz2
    ```

![](img/d23d693b165264e1f2745b298c09e9ab_43.png)

![](img/d23d693b165264e1f2745b298c09e9ab_45.png)

*   **解压缩文件**：同样使用 `-d` 选项。
    ```bash
    bzip2 -d services.bz2
    ```

![](img/d23d693b165264e1f2745b298c09e9ab_47.png)

![](img/d23d693b165264e1f2745b298c09e9ab_48.png)

### 3. xz压缩格式

![](img/d23d693b165264e1f2745b298c09e9ab_50.png)

![](img/d23d693b165264e1f2745b298c09e9ab_52.png)

`xz`工具能提供最高的压缩比例，但压缩速度最慢。

*   **压缩文件**：
    ```bash
    xz services
    ```
    压缩后文件为`services.xz`。查看大小可能仅为 **100K**。

![](img/d23d693b165264e1f2745b298c09e9ab_54.png)

![](img/d23d693b165264e1f2745b298c09e9ab_56.png)

*   **查看压缩文件内容**：使用`xzcat`。
    ```bash
    xzcat services.xz
    ```

![](img/d23d693b165264e1f2745b298c09e9ab_58.png)

*   **解压缩文件**：使用 `-d` 选项。
    ```bash
    xz -d services.xz
    ```

![](img/d23d693b165264e1f2745b298c09e9ab_59.png)

### 三种格式对比总结

| 工具 | 压缩速度 | 压缩比例 | 适用场景 |
| :--- | :--- | :--- | :--- |
| **gzip** | 最快 | 一般 | 需要快速压缩/解压的场景，如流媒体传输 |
| **bzip2** | 中等 | 较高 | 对压缩比有要求，且时间充裕的场景 |
| **xz** | 最慢 | 最高 | 追求极限压缩比，如长期归档备份 |

![](img/d23d693b165264e1f2745b298c09e9ab_61.png)

> **核心思想**：需要被用户频繁访问的文件（如网页视频），常选用压缩速度快的格式；用于备份存储的文件，则可选用压缩比例高的格式。

![](img/d23d693b165264e1f2745b298c09e9ab_63.png)

## tar打包工具 📁

![](img/d23d693b165264e1f2745b298c09e9ab_64.png)

![](img/d23d693b165264e1f2745b298c09e9ab_66.png)

![](img/d23d693b165264e1f2745b298c09e9ab_67.png)

![](img/d23d693b165264e1f2745b298c09e9ab_69.png)

![](img/d23d693b165264e1f2745b298c09e9ab_71.png)

上一节我们介绍了三种压缩工具，但它们都有一个共同的限制：**无法直接对目录进行压缩**。本节中我们来看看如何用`tar`工具解决这个问题。

![](img/d23d693b165264e1f2745b298c09e9ab_73.png)

![](img/d23d693b165264e1f2745b298c09e9ab_75.png)

![](img/d23d693b165264e1f2745b298c09e9ab_77.png)

`tar`是一个**打包工具**，它可以将多个文件或目录合并成一个单独的归档文件（称为`tar`包，后缀为`.tar`）。打包本身**不改变文件大小**，但打包后的单个文件可以方便地使用上述压缩工具进行压缩。

![](img/d23d693b165264e1f2745b298c09e9ab_79.png)

![](img/d23d693b165264e1f2745b298c09e9ab_81.png)

![](img/d23d693b165264e1f2745b298c09e9ab_82.png)

### 基本打包操作

![](img/d23d693b165264e1f2745b298c09e9ab_84.png)

![](img/d23d693b165264e1f2745b298c09e9ab_86.png)

![](img/d23d693b165264e1f2745b298c09e9ab_88.png)

`tar`命令的常用选项：
*   `-c`：创建新的打包文件。
*   `-f`：指定打包后的文件名。**此选项必须放在所有选项的最后**。
*   `-v`：显示详细的打包过程（可选）。

**命令格式**：`tar -cf [打包后的文件名] [要打包的目录或文件]`

例如，将`/opt`目录打包成`opt.tar`：
```bash
tar -cf opt.tar /opt
```
> 注意：系统会提示“从成员名中删除开头的‘/’”，这是为了防止解包时覆盖系统原始路径，是安全特性。

![](img/d23d693b165264e1f2745b298c09e9ab_90.png)

打包后，可以再用`gzip`等工具压缩`opt.tar`文件。

![](img/d23d693b165264e1f2745b298c09e9ab_92.png)

### 打包并压缩（一步到位）

`tar`命令可以结合压缩选项，一次性完成打包和压缩。

*   **调用gzip压缩**：使用 `-z` 选项。
    ```bash
    tar -czf log.tar.gz /var/log
    ```
    此命令将`/var/log`目录打包并压缩为`log.tar.gz`。

*   **调用bzip2压缩**：使用 `-j` 选项。
    ```bash
    tar -cjf log.tar.bz2 /var/log
    ```

*   **调用xz压缩**：使用 `-J` 选项。
    ```bash
    tar -cJf log.tar.xz /var/log
    ```

### 查看打包文件内容

使用 `-t` 选项可以列出打包或压缩包内的文件列表，同样需要 `-f` 指定文件名。
```bash
tar -tzf log.tar.gz
```

### 解包与解压缩

使用 `-x` 选项进行解包/解压，它是通用的。

*   **解压到当前目录**：
    ```bash
    tar -xzf log.tar.gz
    ```

*   **解压到指定目录**：使用 `-C` 选项。
    ```bash
    tar -xzf log.tar.gz -C /opt
    ```
    由于打包时已去除路径开头的“/”，解压时才能自由指定目标位置。

## 总结 📝

![](img/d23d693b165264e1f2745b298c09e9ab_94.png)

本节课中我们一起学习了Linux下的文件压缩与打包。
1.  我们掌握了`gzip`、`bzip2`、`xz`三种压缩工具的使用方法，了解了它们在**压缩速度**和**压缩比例**上的区别。
2.  我们学习了`tar`打包工具的核心用法，包括使用`-c`创建包、`-x`解包、`-t`查看内容，以及至关重要的`-f`选项。
3.  我们重点练习了如何通过`-z`、`-j`、`-J`选项，用`tar`命令一步完成**打包并压缩**，以及如何用`-C`选项将文件解压到指定路径。

![](img/d23d693b165264e1f2745b298c09e9ab_96.png)

![](img/d23d693b165264e1f2745b298c09e9ab_97.png)

熟练运用这些命令，将极大提升你在服务器上进行文件归档、备份和迁移的效率。